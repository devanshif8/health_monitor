# Copy to .env and override for production.
# The JWT secret MUST be changed to a long random string in any real deployment.
DATABASE_URL=sqlite+aiosqlite:///./health_monitor.db
JWT_SECRET=dev-secret-change-me-in-production
