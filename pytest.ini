[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    ml: tests that require the heavy ML stack (Prophet); skipped if unavailable.
filterwarnings =
    ignore::DeprecationWarning
