# Core API + data deps. Tested on Python 3.11–3.14.
# Version floors (not hard pins) — hard pins fail to build on Python 3.14.
fastapi>=0.115
uvicorn[standard]>=0.34
sqlalchemy>=2.0.36
aiosqlite>=0.20
alembic>=1.14
pydantic>=2.10
pydantic-settings>=2.7
email-validator>=2.0
python-dateutil>=2.9

# Auth
python-jose[cryptography]>=3.3
bcrypt>=4.1

# Data / analytics (imported by app.services + routers)
pandas>=2.0
numpy>=1.24
